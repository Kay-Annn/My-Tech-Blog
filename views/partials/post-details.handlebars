{{!-- <div class="card mb-6">
    <header class="card-header bg-warning">
        <div class="row">
            <p class=" has-text-primary-light"></p>
            <a href="api/post/{{id}}" class="has-text-black">{{title}} </a></p>
        </div>
        <div class="is-pulled-left">
            <b>{{username}}</b>
            {{format_date created_at}} | <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment"
                comments.length}}</a>
        </div>
        <div class="row">
            <span class=""> </span>
        </div>
    </header>
    <div class="card-content">
        <div class="has-text-justified">
            {{content}}
        </div>
    </div> --}}